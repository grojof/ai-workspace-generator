/** Version of the template set shipped by this CLI. Bump when templates change. */
export const TEMPLATES_VERSION = "0.32.0";
/**
 * The CLI's own version, reported by `ai-workspace --version` and used to name the release tarball.
 * MUST stay in sync with `package.json.version` — `test/version.test.js` enforces this.
 */
export const CLI_VERSION = "0.1.0";

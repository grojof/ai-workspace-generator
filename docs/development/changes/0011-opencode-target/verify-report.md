# Verify — `opencode` target

Verified against `npm run build` + `npm test` (79/79) + `ai-workspace doctor` + a real generation sample.

| Req | Scenario | Result |
|-----|----------|--------|
| R1 | `opencode` is a selectable target | ✅ schema enum widened; offered in both wizard multiselects; casts/unions widened |
| R2 | AGENTS.md is the adapter, no extra instructions file | ✅ `targets:[opencode]` ⇒ AGENTS.md generated; no CLAUDE.md / copilot files (test) |
| R3 | MCP → tool-owned `.opencode/opencode.json` (gated) | ✅ real generation: `{ "$schema":"https://opencode.ai/config.json", "mcp": { "context7": { "type":"local","command":["npx","-y","@upstash/context7-mcp"],"enabled":true } } }`; only `$schema`+`mcp` keys |
| R3 | Empty MCP ⇒ no file | ✅ test: `mcp:[]` ⇒ `.opencode/opencode.json` absent, AGENTS.md alone |
| R4 | Idempotency | ✅ second generate = 0 changed (test) |
| R5 | Docs | ✅ targets table row (README EN/ES + USAGE EN/ES) + a full OpenCode subsection with per-tool notes and a "things to keep in mind" list |
| — | doctor / budgets | ✅ 0 errors / 0 warnings (AGENTS.md content unchanged by this target) |

## Notes
- **Skills need zero work:** OpenCode discovers `.claude/skills/<name>/SKILL.md` natively (context7-confirmed),
  so the `claude` target's skills are reused as-is — documented, not re-emitted.
- **No clobber:** OpenCode deep-merges config, so the dedicated `.opencode/opencode.json` (regenerated by sync)
  combines with the user's own `opencode.json`/global config. Docs tell users to keep their settings there.
- `TEMPLATES_VERSION` → **0.35.0** (generated surface changed: new target + wizard option). AGENTS.md block
  output is unchanged, so the byte-baseline fixtures are untouched.
- **Deferred (out of scope):** projecting slash commands to `.opencode/command/<name>.md` — a possible follow-up.

## Status
**Ready to commit.** Archive after merge.

# Tasks — F1: `aiws-` namespacing + provenance

## F1a.1 — skills + registry + provenance + guard
**Increment 1 (done):** provenance + naming helper.
- [x] `src/generate/naming.ts`: `AIWS`, `aiwsId()`, `isReservedNamespace()`, `skillFrontmatter()` (with `source:`).
- [x] Centralised `frontmatter()` → `skillFrontmatter` in `skills.ts` + `guides.ts` + `governance.ts`: every generated skill now carries `source: aiws@<TEMPLATES_VERSION>`. Verified (`source: aiws@0.36.0`).
- [x] Bump `TEMPLATES_VERSION` → 0.36.0; `npm test` green (79/79).

**Increment 2 (done):** the `aiws-` rename (collision fix, fixture-touching).
- [x] Skill emission → `aiwsId`: `skills.ts` (sdd-* via `aiwsId(p.name)` + `aiws-living-docs`), `governance.ts` (`aiws-secure-commit`, `aiws-dependency-upgrade`), `guides.ts` (`aiws-workspace-guide`, `aiws-configure-workspace`, `aiws-vscode-setup`), `learning.ts` (`aiws-learn` + frontmatter centralised) — folder + `name` + body heading + in-body skill-name refs.
- [x] `src/modules/skills.ts`: 8 non-pack registry ids → `aiws-*` (pack rows left for F2).
- [x] `templates/core/routing.md.eta`: skill-name references → `aiws-*`.
- [x] Tests: `generate.test.js` + `multi-repo.test.js` skill-path asserts → `aiws-*`. Regenerated `test/__fixtures__/agents/*.md` (skill-routing block only; block-order golden unchanged). 79/79 green; `doctor` 0/0 (AGENTS.md 5787/6000). Verified: all base skills emit as `aiws-*` (packs `find-skills`/`mcp-builder`/`skill-creator` remain → F2).
- [x] Reserved-namespace guard (+ test) — **landed in F2c** (`packsSync` reserved-namespace guard, change 0014).

## F1a.2 — commands + prose sweep (done)
- [x] Command/prompt files + identifiers → `/aiws-*`: `sdd.ts` (`/aiws-sdd-*`, `/aiws-sdd-sync`), `governance.ts` (`/aiws-commit`, `/aiws-upgrade-deps`), `guides.ts` (`/aiws-configure`), `learning.ts` (`/aiws-learn`), `livingDocs.ts` (`/aiws-doc-sync`).
- [x] Prose sweep: 9 templates (`workflow`, `conventions`, `learning`, `sdd/orchestrator`, `living-docs/section`, `business/domain`, `shared/ai-workspace` EN+ES, `targets/claude/CLAUDE`) + generator strings (`skills.ts`, `docsIndex.ts`, `index.ts` Stop-hook, `i18n/strings.ts`) + CLI notes (`init.ts`).
- [x] Guard test in `invariants.test.js`: scans all generated `.md`/`.json`, fails on any legacy command token (precise regex ignores file paths like `_shared/sdd-convention.md`). Updated `generate`/`methodology`/`package` command-path asserts. Regenerated byte baselines.
- [x] 80/80 green; `doctor` 0/0 (AGENTS.md 5787/6000). No unprefixed skill-name leaks (lookbehind grep clean).

## F1b — block-id namespace + migration (done)
**F1b.1 (done):** namespace the governance-spine block ids.
- [x] Centralised `aiws:` prefix in `composeFromManifest` (covers static + dynamic `lang-*`/`fw-*`/`env-*` ids); inline `claude`/`copilot-header` via `aiwsBlockId`. New `aiwsBlockId()` in `naming.ts`.
- [x] Widened `doctor` orphan-scan regex (`[\w:-]+`) and `remove` strip ids (`aiwsBlockId(lang-*)`).
- [x] Golden order + Layer-0 prefix + tech-selection/skill-routing asserts → `aiws:*`; regenerated 5 byte baselines; widened invariants `blockOrder` regex; `block-manifest` mirrors the namespace via `aiwsBlockId`.
- [x] Bumped `TEMPLATES_VERSION` → 0.37.0. 80/80 green; `doctor` reports no orphans on a namespaced repo.

**F1b.2 (done):** automated upgrade migration.
- [x] `src/commands/migrate.ts`: `migrateBlockIds` (config-derived legacy→`aiws:` marker rewrite across AGENTS.md/CLAUDE.md[/per-repo]/copilot, anchored + idempotent) + `pruneRenamedOrphans` (removes pre-rename skill folders / command + prompt files, guarded by the current artifact set).
- [x] Wired into `runUpgrade`: migrate markers **before** render, prune **after**; both reported, skipped under `--check`.
- [x] `test/migrate.test.js` (3 tests): in-place namespacing + user-prose survival; idempotency + no duplicate blocks on re-generate; prune removes legacy skills/commands while keeping `aiws-*`, user skills, and un-renamed packs. 83/83 green.
- [x] MAINTAINING note: the `aiws:` namespace + automated migration.

## Verify
- [ ] `verify-report.md` after F1a.1.

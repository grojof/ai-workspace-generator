# Migrated from src/generate/sddAudits.ts (skills-as-data, 0003 phase C). The SKILL.md is the captured
# generator output (byte-equivalent). Routing stays in the catalog (src/modules/skills.ts) → routing: false.
id: sdd-audit-style
gating:
  sdd:
    enabled: true
    vendorSkills: true
    schema: reasons
profile:
  userType: [business, technical]
routing: false
loadMode: advanced-only

---
description: Write requirements & scenarios (delta spec).
---
# /aiws-sdd-spec
Run the **`aiws-sdd-spec`** skill — it carries the inputs to read, the `spec.md` template, and the quality bar for this phase.
> Artifacts are versioned in `docs/development/` and reviewed in PRs.

---
description: Implement tasks following spec & design.
---
# /sdd-apply
Implement tasks in order, checking them off in `tasks.md`. Keep code aligned with the spec.
> Artifacts are versioned in `docs/development/` and reviewed in PRs.
Follow the SDD lifecycle in AGENTS.md. Read prior artifacts in the change folder before writing the next one.

---
description: Validate implementation against spec.
---
# /aiws-sdd-verify
Run the **`aiws-sdd-verify`** skill — it carries the inputs to read, the `verify-report.md` template, and the quality bar for this phase.
> Artifacts are versioned in `docs/development/` and reviewed in PRs.

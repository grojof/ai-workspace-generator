---
description: Break the change into a task checklist.
---
# /aiws-sdd-tasks
Run the **`aiws-sdd-tasks`** skill — it carries the inputs to read, the `tasks.md` template, and the quality bar for this phase.
> Artifacts are versioned in `docs/development/` and reviewed in PRs.

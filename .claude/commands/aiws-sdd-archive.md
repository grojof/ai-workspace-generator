---
description: Fold the change into main specs and archive.
---
# /aiws-sdd-archive
Run the **`aiws-sdd-archive`** skill — it carries the inputs to read, the `specs/ (updated) + archive/<date-name>/` template, and the quality bar for this phase.
> Artifacts are versioned in `docs/development/` and reviewed in PRs.

---
description: Implement tasks following spec & design.
---
# /aiws-sdd-apply
Run the **`aiws-sdd-apply`** skill — it carries the inputs to read, the `tasks.md (kept current)` template, and the quality bar for this phase.
> Artifacts are versioned in `docs/development/` and reviewed in PRs.
